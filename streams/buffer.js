const buf = Buffer.from('hello');

console.log(buf.toJSON());

// **é um objeto que armazena dados binários em memória temporária**

// . É uma estrutura de dados que permite a manipulação direta da memória bruta, o que o torna eficiente em termos de desempenho.

// . O buffer é uma estrutura de dados que armazena dados em um array de bytes.